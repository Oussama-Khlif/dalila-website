from django.apps import AppConfig

class HumanRessorcesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'human_ressorces'  # This should match the app's directory name

